<div class="row"> 
  <% @images.each do |image| %>
    <div class="span2 gallery_images">
      <%= link_to image_tag(image.file_location, :alt => '', :class => "img-polaroid2"), "/assets/"+image.file_location, {:rel => "group", :class => "fancybox"} %>
    </div>
  <% end %>  
</div>